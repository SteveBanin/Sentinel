apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-service
  namespace: sentinel
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-service
  template:
    metadata:
      labels:
        app: api-service
    spec:
      containers:
        - name: api-service
          image: sentinel/api-service:dev
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
            - containerPort: 8080
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"

          env:
            - name: DB_HOST
              value: "sentinel-postgres.sentinel.svc.cluster.local"
            - name: DB_PORT
              value: "5432"
            - name: DB_NAME
              value: "sentinel"
            - name: DB_USER
              value: "sentinel"
            - name: DB_PASSWORD
              value: "sentinel"
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 20